# =========================================================================
# AssistaJunto - Template de Variaveis de Ambiente
# =========================================================================
# DESENVOLVIMENTO LOCAL (dotnet run): use as URLs de localhost.
# PRODUCAO (docker compose up):       troque pelas URLs de dominio real.
# =========================================================================

# --- Banco de Dados ---
POSTGRES_USER=postgres
POSTGRES_PASSWORD=
POSTGRES_DB=assistajunto
# Local: Host=localhost | Docker: o docker-compose sobrescreve para Host=postgres
ConnectionStrings__DefaultConnection=Host=localhost;Port=5432;Database=assistajunto;Username=postgres;Password=

# --- Discord OAuth ---
Discord__ClientId=
Discord__ClientSecret=
# Local:
Discord__RedirectUri=https://localhost:7045/api/auth/callback
# Producao: Discord__RedirectUri=https://api.assistajunto.com.br/api/auth/callback

# --- JWT ---
Jwt__Secret=
Jwt__Issuer=AssistaJunto
Jwt__Audience=AssistaJuntoClient
Jwt__ExpirationInHours=24

# --- URLs ---
# Local:
ClientUrl=https://localhost:7036
API_BASE_URL=https://localhost:7045
# Producao:
# ClientUrl=https://www.assistajunto.com.br
# API_BASE_URL=https://api.assistajunto.com.br

# --- Traefik / Let's Encrypt (producao) ---
ACME_EMAIL=seu@email.com

# --- ZeroTier (opcional - rede local virtual) ---
# ZeroTier__ApiUrl=
# ZeroTier__ClientUrl=
